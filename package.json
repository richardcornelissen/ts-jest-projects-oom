{
  "name": "ts-jest-projects-oom",
  "version": "1.0.0",
  "description": "Example repo for out of memory issues with ts-jest and jest projects",
  "main": "index.js",
  "author": "Richard Cornelissen",
  "license": "MIT",
  "scripts": {
    "test": "yarn jest --config ./jest.config.js --logHeapUsage --runInBand --no-cache",
    "test:isolated-modules": "IM=true yarn jest --config ./jest.config.js --logHeapUsage --runInBand --no-cache"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "aws-sdk": "^2.769.0",
    "jest": "^26.5.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  }
}
